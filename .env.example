# ===========================================
# NEWS PORTAL - ENVIRONMENT CONFIGURATION
# ===========================================
# These are SERVER-SIDE secrets and configurations that should NOT be in database
# Things like API keys, database credentials, and secrets

# ===========================================
# Database (REQUIRED - cannot be dynamic)
# ===========================================
MONGODB_URI=

# ===========================================
# Admin Authentication (REQUIRED - defaults for first run)
# ===========================================
ADMIN_SECRET
ADMIN_EMAIL=
ADMIN_PASSWORD=

# ===========================================
# API Keys (REQUIRED - secrets, cannot be in DB)
# ===========================================
# OpenAI for AI content generation
OPENAI_API_KEY=

# BrightData for web scraping
BRIGHTDATA_API_TOKEN=
BRIGHTDATA_ZONE=

# ===========================================
# DIMOCO Payment - API Credentials (REQUIRED for production)
# ===========================================
# These are secrets - URLs are now DYNAMIC from request
# DIMOCO Payment - Sandbox Credentials
DIMOCO_API_URL=
DIMOCO_MERCHANT_ID=
DIMOCO_PASSWORD=
DIMOCO_ORDER_ID=
# NOTE: DIMOCO_SUCCESS_URL and DIMOCO_CANCEL_URL removed - now dynamic from request

# ===========================================
# Environment
# ===========================================
NODE_ENV=development

# ===========================================
# DCB / Network Detection (for testing)
# ===========================================
# Bypass network validation to allow purchases on WiFi (for testing only!)
# Set to 'false' or remove in production
BYPASS_NETWORK_CHECK=false

# ===========================================
# Default Settings (used on first run, then stored in DB)
# ===========================================
# Brand defaults
BRAND_ID=brand1
BRAND_NAME=News Portal
BRAND_LOGO_URL=/images/logo.svg
BRAND_FAVICON_URL=/favicon.svg
BRAND_PRIMARY_COLOR=#1a73e8
BRAND_SECONDARY_COLOR=#ea4335

# Pricing defaults
ARTICLE_PRICE_CENTS=99
PRICING_ENABLED=false
PRICING_CURRENCY=EUR

# Agent defaults
AGENT_ENABLED=false
AGENT_INTERVAL_MINUTES=60
AGENT_MODEL=gpt-4o-mini
AGENT_TEMPERATURE=0.7
AGENT_MAX_ARTICLES=5
AGENT_RSS_FEEDS=https://feeds.bbci.co.uk/news/rss.xml

# ===========================================
# NOTE: These values are DEFAULTS for first app run.
# Once saved in admin panel, the database values take priority.
# Dynamic values (domain URLs) are always derived from request headers.
